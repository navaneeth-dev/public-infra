---
- name: Deploy all public infrastructre services
  hosts: vps
  remote_user: debian
  vars:
    primary_user: debian
    primary_group: debian
    install_directory: /srv
    data_dir: /docker/appdata
  roles:
    - role: geerlingguy.docker
      become: yes
    - role: commafeed
